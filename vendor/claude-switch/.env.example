# claude-qwen connection + secret. Copy to .env (chmod 600) and fill API_KEYS.
# The MODEL LINEUP is NOT here — it lives in models.env (version-controlled).
BASE_URL='https://token-plan.ap-southeast-1.maas.aliyuncs.com/apps/anthropic'
API_KEYS='sk-sp-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
